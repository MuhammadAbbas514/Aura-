import React from 'react';

const HeroSection = () => (
  <div className="hero-section">
    <div className="hero-overlay"></div>
    <h2 className="hero-text">Explore Yourself</h2>
  </div>
);

export default HeroSection;
